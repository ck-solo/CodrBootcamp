statelifting up 
it is used to get data from child to parents


context api

createcontext ek global context hai jo har jagah data bhej skta hai 
createcontext

then save createcontext()

let contextProvider = () =>{
    return <mystore.provider></mystore.provider>
}

