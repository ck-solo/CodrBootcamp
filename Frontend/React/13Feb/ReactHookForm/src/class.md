registered 
handleSubmit
reset,
forstate:{errors}, 
required
minlenght

{...registered("name"),{
    required:"Name is required",
    minLength:{

    }
}}


task
make login and registered form 
get values from via react hook form

save that user into localstorage
now login the user which is saved inyour register array in local storage 

if user found display their name is App.jsx