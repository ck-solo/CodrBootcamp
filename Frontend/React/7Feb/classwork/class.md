cdn -> kya hota hai?
cors => crossorigin is used when third party start to intereact with our domain
es module => helps to connect multiple folders and files
behind the seen how react work
babel => a javascript compiler

pakcage-lock js => all dependcies are inside that whe
let h1 = React.createElement("h1",{},"this is react");
console.log(h1)

setup the package.json by npm init
package-lock.json ->npm i -D vite